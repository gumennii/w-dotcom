import { type ResolveComponentResultWithType } from "~/uniform/models";
import { Button } from "./Button";

export const buttonMapping: ResolveComponentResultWithType = {
  type: "button",
  component: Button,
};
