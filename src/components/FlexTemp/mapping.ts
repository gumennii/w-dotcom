import { type ResolveComponentResultWithType } from "~/uniform/models";
import { FlexTemp as Flex } from "./FlexTemp";

export const flexMapping: ResolveComponentResultWithType = {
  type: "flex",
  component: Flex,
};
